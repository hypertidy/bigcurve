% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/densify.R
\name{densify}
\alias{densify}
\title{Adaptively densify lines for a projection}
\usage{
densify(
  x,
  target,
  source = "OGC:CRS84",
  tolerance = NULL,
  pixels = 2048L,
  max_depth = 16L
)
}
\arguments{
\item{x}{a two-column matrix of lon,lat (a path), a list of such
matrices, a segment mesh (a list with \code{vb}, a 4 x n vertex matrix
with rows x, y, z, h, and \code{is}, a 2 x m segment index matrix,
1-based, as used by rgl's mesh3d), or a \code{wkpool} vertex-pool
topology from the wkpool package (any wk-handleable geometry via
\code{wkpool::establish_topology()}); pools carrying z or m vertex values
are refused, as densification cannot invent them at new vertices}

\item{target}{projection for which to densify (proj string, WKT,
authority code - anything PROJ accepts)}

\item{source}{coordinate system of the input, default 'OGC:CRS84'
(longitude, latitude); authority codes are axis-normalized so
'EPSG:4326' input is still given as lon,lat}

\item{tolerance}{maximum allowed deviation, in units of \code{target}
(usually metres); if \code{NULL} (the default) it is derived from the
projected extent of the input as \code{min(diff(range))/pixels}}

\item{pixels}{notional output width/height used to derive the default
\code{tolerance}, i.e. "keep error under about one part in \code{pixels} of the
plot"; ignored when \code{tolerance} is supplied}

\item{max_depth}{maximum bisection depth per input segment (each level
at most doubles the vertex count of a segment)}
}
\value{
the same kind of object as \code{x}, with vertices added: a matrix
for a path, a list of matrices for a list, a mesh with appended
vertices and refined \code{is} for a mesh, a \code{wkpool} with appended
vertices (freshly minted \code{.vx} ids), refined directed segments, and
\code{.feature} provenance carried onto every refined segment
}
\description{
Insert vertices along great circle arcs exactly where a target projection
bends them, so that straight chords drawn between output vertices stay
within \code{tolerance} of the true projected curve. This is the adaptive
resampling scheme of d3-geo (Bostock), generalized to paths and to
segment meshes.
}
\details{
The error metric is evaluated in the plane of the \code{target} projection:
the great circle midpoint of each candidate segment is forward-projected
and compared to the segment's planar chord (perpendicular distance, plus
a parametric check that catches strong shear). Segments are split
recursively until they pass or \code{max_depth} is reached. No inverse
transforms are used and each point is projected exactly once.

Coordinates are returned in the coordinates of \code{source} (geographic by
default): project the result yourself for plotting, e.g. with
\code{\link[=rproj_xy]{rproj_xy()}} or your tool of choice.

Points that cannot be projected (for example beyond the horizon of an
orthographic projection) do not error: segments touching them are left
un-densified, and refinement stops at unprojectable midpoints. Clipping
is out of scope here.

Inserted vertices are emitted in the longitude frame of their input
edge: an edge along longitude 180 refines at 180, its mirror at -180
refines at -180 (the two seams of a global layer remain distinct), and
an edge written as crossing the antimeridian yields a locally
continuous chain, possibly outside \verb{[-180, 180)}. Input vertices are
always returned with their written values, so a crossing edge's path
carries one numeric 360-degree step at its far endpoint (the same
point on the sphere; PROJ is indifferent). Longitudes are never
canonicalized; normalize afterwards if a canonical range is needed.
Edges that cross the \emph{target} projection's own discontinuity (which
depends on \code{lon_0}) cannot form a valid planar chain and should be cut
beforehand; refinement of such edges is bounded, not exploded, because
arcs spanning less ground than \code{tolerance} are never split.
}
\examples{
s <- segment(c(140, -164), c(-89, 80))
d <- densify(s, "+proj=laea +lon_0=147 +lat_0=-42")
nrow(d)
plot(rproj_xy(d, "+proj=laea +lon_0=147 +lat_0=-42"), type = "l", asp = 1)
}
\references{
\url{https://bost.ocks.org/mike/example/}
}
